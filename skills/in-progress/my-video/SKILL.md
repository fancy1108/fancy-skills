---
name: my-video
description: Stub. Do not run. Video production pipeline to extract from Fancy's my-video project later.
disable-model-invocation: true
---

# My Video (stub)

**Refuse to run.** This skill is a placeholder so the work is not forgotten. It is not in the plugin. Tell the user it is not shipped and stop. Do not generate video, install ComfyUI, or invent a workflow.

## What it will be

A thin `SKILL.md` plus scripts that wrap Fancy's existing **my-video** production project (heavy dependencies, not a prompt-only skill). Extract later; do not copy the whole project into this repo.

When filling this in:

1. List the real entry commands and required binaries/services.
2. Keep secrets and large weights out of git.
3. Put long reference in `reference.md`; keep `SKILL.md` as the router.
4. Then promote into `engineering/` or `productivity/` via [`.agents/adding-a-skill.md`](../../../.agents/adding-a-skill.md) and delete this stub.
